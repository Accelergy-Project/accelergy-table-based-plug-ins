version: 0.1
sets_of_tables:                          # A (lower case) name for the plugin (not really used elsewhere)
    module: sets_of_tables               # The (lower case) Python module name, which actually is the name of the file
    class: SetsOfTables                  # The class name

